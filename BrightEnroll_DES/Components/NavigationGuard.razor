@using BrightEnroll_DES.Services.AuthFunction
@using BrightEnroll_DES.Services
@inject IAuthService AuthService
@inject ILoadingService LoadingService
@inject NavigationManager Navigation
@implements IDisposable

@code {
    protected override void OnInitialized()
    {
        // Subscribe to navigation events
        Navigation.LocationChanged += OnLocationChanged;
        
        // Check initial location
        CheckAndRedirect();
    }

    private void OnLocationChanged(object? sender, Microsoft.AspNetCore.Components.Routing.LocationChangedEventArgs e)
    {
        CheckAndRedirect();
    }

    private void CheckAndRedirect()
    {
        var currentUri = Navigation.Uri;
        var currentUser = AuthService.CurrentUser;

        // If user is logged in and tries to navigate to login page, redirect to appropriate dashboard
        if (AuthService.IsAuthenticated && currentUri.Contains("/login"))
        {
            if (currentUser?.user_role == "Super Admin")
            {
                Navigation.NavigateTo("/system-admin/dashboard", forceLoad: true);
            }
            else if (currentUser?.user_role == "Teacher")
            {
                Navigation.NavigateTo("/teacher/dashboard", forceLoad: true);
            }
            else if (currentUser?.user_role == "Cashier")
            {
                Navigation.NavigateTo("/cashier/dashboard", forceLoad: true);
            }
            else
            {
                Navigation.NavigateTo("/dashboard", forceLoad: true);
            }
            return;
        }

        // Redirect teachers trying to access admin, cashier, or super admin pages
        if (AuthService.IsAuthenticated && currentUser?.user_role == "Teacher")
        {
            var adminRoutes = new[] 
            { 
                "/dashboard", 
                "/enrollment", 
                "/student-record", 
                "/academics", 
                "/finance", 
                "/human-resource", 
                "/archive", 
                "/audit-log" 
            };

            var uri = new Uri(currentUri);
            var path = uri.AbsolutePath;
            
            if (adminRoutes.Any(route => path.StartsWith(route, StringComparison.OrdinalIgnoreCase)) ||
                path.StartsWith("/cashier", StringComparison.OrdinalIgnoreCase) ||
                path.StartsWith("/system-admin", StringComparison.OrdinalIgnoreCase))
            {
                Navigation.NavigateTo("/teacher/dashboard", forceLoad: true);
            }
        }

        // Redirect cashiers trying to access admin, teacher, or super admin pages
        if (AuthService.IsAuthenticated && currentUser?.user_role == "Cashier")
        {
            var adminRoutes = new[] 
            { 
                "/dashboard", 
                "/enrollment", 
                "/student-record", 
                "/academics", 
                "/human-resource", 
                "/archive", 
                "/audit-log" 
            };

            var uri = new Uri(currentUri);
            var path = uri.AbsolutePath;
            
            if (adminRoutes.Any(route => path.StartsWith(route, StringComparison.OrdinalIgnoreCase)) ||
                path.StartsWith("/teacher", StringComparison.OrdinalIgnoreCase) ||
                path.StartsWith("/system-admin", StringComparison.OrdinalIgnoreCase))
            {
                Navigation.NavigateTo("/cashier/dashboard", forceLoad: true);
            }
        }

        // Redirect school admins trying to access teacher, cashier, or super admin pages
        if (AuthService.IsAuthenticated && currentUser?.user_role == "Admin")
        {
            var uri = new Uri(currentUri);
            var path = uri.AbsolutePath;
            
            if (path.StartsWith("/teacher", StringComparison.OrdinalIgnoreCase) ||
                path.StartsWith("/cashier", StringComparison.OrdinalIgnoreCase) ||
                path.StartsWith("/system-admin", StringComparison.OrdinalIgnoreCase))
            {
                Navigation.NavigateTo("/dashboard", forceLoad: true);
            }
        }

        // Redirect super admins trying to access school admin, teacher, or cashier pages
        if (AuthService.IsAuthenticated && currentUser?.user_role == "Super Admin")
        {
            var schoolAdminRoutes = new[] 
            { 
                "/dashboard", 
                "/enrollment", 
                "/student-record", 
                "/academics", 
                "/finance", 
                "/human-resource", 
                "/archive", 
                "/audit-log" 
            };

            var uri = new Uri(currentUri);
            var path = uri.AbsolutePath;
            
            if (schoolAdminRoutes.Any(route => path.StartsWith(route, StringComparison.OrdinalIgnoreCase)) ||
                path.StartsWith("/teacher", StringComparison.OrdinalIgnoreCase) ||
                path.StartsWith("/cashier", StringComparison.OrdinalIgnoreCase))
            {
                Navigation.NavigateTo("/system-admin/dashboard", forceLoad: true);
            }
        }
    }

    public void Dispose()
    {
        Navigation.LocationChanged -= OnLocationChanged;
    }
}

